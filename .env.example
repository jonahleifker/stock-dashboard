PORT=6405
NODE_ENV=development
SQLITE_STORAGE=./var/dev.sqlite
SESSION_SECRET=your-session-secret-here
JWT_SECRET=your-jwt-secret-here
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# Backend auth bypass flag for development (set to true to bypass authentication)
DEV_AUTH_BYPASS=false

# Database Configuration
# Using SQLite by default. Uncomment DATABASE_URL to use PostgreSQL instead.
# DATABASE_URL=postgresql://postgres:your-password@db.your-project-ref.supabase.co:5432/postgres

# File Storage
# Files are stored locally in ./var/uploads/ directory
# This works with Railway when you mount a persistent volume at /app/var
